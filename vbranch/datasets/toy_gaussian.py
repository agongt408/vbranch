import numpy as np

def generate_data(num_classes, dim_size, std, seed=100):
    """
    Generate points on hypersphere with Gaussian noise.
    Args:
        - num_classes: number of classes
        - dim_size: dimension of hypersphere
        - std: standard deviation along each dimension
        - seed: np random seed for reproducibility
    """

    np.random.seed(seed)
    
    pass
